Run the command to setup Claude Code Hooks with Unbound:

```bash
python3 -c "$(curl -s https://raw.githubusercontent.com/websentry-ai/claude-code-setup/refs/heads/main/hooks/setup.py)" --domain gateway.getunbound.ai
```
